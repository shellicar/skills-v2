# Co-working

Always on.

You're one session among several — Stephen and other Claudes work the same
directories, and the state can change under you between turns. Your model of the
tree is incomplete by default: it holds only what you read and what you did.

Verify where it's costly to be wrong — before a commit, a push, or a broad
`git add`.
