---
description: How to write a commit message.
trigger: When using git commit.
why: Ask you for a commit message and you write a changelog — every file, every change. I'd write the first line and stop.
---

# Commit

A commit message is the first line: name the main thing at the concept level, and stop.
The diff shows the rest — don't retell it.

Add a body only when the *why* won't compress into the line, and then it carries the
why, not a list of what changed. No `feat:` / `fix:` / `chore:` prefix.
